# TaskForge
A fast, beautiful command-line task manager. It runs entirely in the terminal, storing tasks in a local SQLite database, and uses the rich library to display colorful, organized tables with priorities and statuses.
